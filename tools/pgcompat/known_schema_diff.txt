# Allowlist of known schema differences between MySQL schema.sql and the PG
# baseline. Each line is either:
#
#   mysql-only: <table>   — table exists only in server/datastore/mysql/schema.sql
#   pg-only:    <table>   — table exists only in server/datastore/mysql/pg_baseline_schema.sql
#
# Blank lines and lines starting with # are ignored. Entries in this file are
# intentional drift — e.g., PG-specific infrastructure tables that have no
# MySQL counterpart, or MySQL tables that are renamed in the PG schema.
#
# If a table appears in the drift diff that is NOT listed here, CI fails and
# the pg_baseline_schema.sql must be regenerated (see its file header for the
# canonical pg_dump command) or the entry must be added here with explanation.

# MySQL-only tables renamed in PG:
#   scep_certificates  → identity_certificates  (renamed for NanoMDM/identity refactor)
#   scep_serials       → identity_serials       (same refactor)
mysql-only: scep_certificates
mysql-only: scep_serials

# PG-only tables:
#   identity_certificates, identity_serials — see rename above.
#   activities, host_activities — PG-native activity log tables (MySQL uses a different shape).
#   acme_enrollments — ACME protocol PG-specific storage.
#   host_managed_local_account_passwords — PG-specific (TODO verify if MySQL equivalent exists under different name).
#   migration_status_data — PG migration tracking (MySQL uses migration_status_tables).
#   user_api_endpoints — PG-specific API endpoint audit.
pg-only: identity_certificates
pg-only: identity_serials
pg-only: activities
pg-only: host_activities
pg-only: acme_enrollments
pg-only: host_managed_local_account_passwords
pg-only: migration_status_data
pg-only: user_api_endpoints

# Allowlist of known schema differences between MySQL schema.sql and the PG
# baseline. Each line is either:
#
#   mysql-only: <table>   — table exists only in server/datastore/mysql/schema.sql
#   pg-only:    <table>   — table exists only in server/datastore/mysql/pg_baseline_schema.sql
#
# Blank lines and lines starting with # are ignored. Entries in this file are
# intentional drift — e.g., PG-specific infrastructure tables that have no
# MySQL counterpart, or MySQL tables that are added upstream after the last
# PG baseline regeneration.
#
# If a table appears in the drift diff that is NOT listed here, CI fails and
# the pg_baseline_schema.sql must be regenerated (see its file header for the
# canonical pg_dump command) or the entry must be added here with explanation.

# PG-only table — no MySQL equivalent.
pg-only: migration_status_data

# 2026-08 upstream sync: new tables created by post-marker migrations (and one
# table dropped upstream) — regenerate the baseline from prod after the next
# deploy, then remove these entries.
mysql-only: host_autopilot_devices
mysql-only: host_mdm_apple_device_vitals
mysql-only: host_mdm_apple_service_subscriptions
mysql-only: mdm_apple_ddm_activations
mysql-only: mdm_microsoft_graph_credentials
mysql-only: mdm_windows_enrollment_config
mysql-only: scim_group_group
pg-only: mdm_apple_declaration_activation_references
